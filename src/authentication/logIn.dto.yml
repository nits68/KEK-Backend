# LINK ./LogIn.dto.ts
components:
    schemas:
        LoginData:
            type: object,
            properties:
                email:
                    type: string
                    description: "A felhasználó e-mail címe"
                    example: "admin@jedlik.eu"
                password:
                    type: string
                    description: "A felhasználó jelszava"
                    example: "admin"
